sonar.projectKey=3dollar-in-my-pocket_3dollars-in-my-pocket-ios
sonar.organization=3dollar-in-my-pocket

# 이 레포는 SonarCloud Automatic Analysis 로 돈다(레포에 스캐너 워크플로가 없다).
# Automatic Analysis 는 이 파일에서 sources/exclusions 계열만 읽고
# sonar.issue.ignore.* (룰 단위 제외)는 읽지 않는다 — 그래서 경로 제외로 처리한다.

# Tuist 생성물·서드파티 체크아웃은 분석 대상이 아니다.
sonar.exclusions=**/Derived/**,Tuist/.build/**,**/*.generated.swift,App/Targets/three-dollar-in-my-pocketTests/**

# 테스트 소스를 분석에서 빼는 이유:
# - swift:S100(함수 네이밍): 이 레포는 테스트 메서드명을 `test_TC{n}_{조건}_{기대결과}` 한글 형식으로
#   강제한다(docs/process/testing.md). 이 이름이 곧 TC 커버리지 표의 근거라 규칙과 양립할 수 없다.
# - swift:S1075(하드코딩 URI): AppEnvironmentTests 는 운영/개발 API URL 자체가 검증 대상이라 리터럴이 필요하다.
# 프로덕션 코드는 그대로 분석된다.
